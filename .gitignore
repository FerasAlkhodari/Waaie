# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.coverage
htmlcov/

# Environment — NEVER commit real secrets (GEMINI_API_KEY lives here)
.env
.env.*
!.env.example
.env.local
backend/.env
backend/.env.local

# Secret / credential config files
*.secret
.secret
.secrets/
*.pem
*.key

# Node (frontend has its own .gitignore too)
node_modules/

# Editor / OS
.DS_Store
.vscode/
.idea/
